/** Aktif oyun snapshot şema sürümü (migration / parse için). */
export const ACTIVE_GAME_SNAPSHOT_SCHEMA_VERSION = 1;

/** Tamamlanan oyun snapshot şema sürümü. */
export const COMPLETED_GAME_SNAPSHOT_SCHEMA_VERSION = 1;

export const DATABASE_NAME = 'yazboz.db';
